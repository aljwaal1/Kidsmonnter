# مراجعة KidsMonnter وخطة الإصلاح

تاريخ بدء المراجعة: 2026-07-24

## الهدف

إعادة بناء موثوقية التطبيق قبل إضافة مزايا جديدة، مع التركيز على:

- دقة احتساب وقت استخدام الهاتف.
- استمرار الحماية بعد إغلاق الواجهة وإعادة تشغيل الهاتف.
- ظهور شاشة القفل عند انتهاء الوقت.
- وضوح الصلاحيات المطلوبة وحالتها.
- منع اختلاف الكود الموجود في المستودع عن الكود الذي يُبنى داخل APK.
- إضافة اختبارات وتشخيص يسمحان بمعرفة سبب الفشل على الجهاز.

## ما تمت مراجعته

- `lib/main.dart`
- `native/MainActivity.kt`
- `native/AndroidManifest.xml`
- `.github/workflows/build-apk.yml`
- `tool/fix_activation.py`
- `pubspec.yaml`

## النتائج الحرجة

### 1. الكود المصدر لا يطابق APK المبني

ملف workflow ينشئ مشروع Android أثناء البناء، ثم ينسخ الملفات الأصلية، وبعد ذلك يشغّل:

```text
python3 tool/fix_activation.py
```

السكربت يغيّر منطق زر تفعيل الحماية داخل نسخة البناء فقط. نتيجة ذلك:

- الكود الذي يراجعه المطور داخل `lib/main.dart` ليس بالضرورة الكود الموجود داخل APK.
- قد ينجح APK بينما يبدو المصدر معطلًا، أو يحدث العكس.
- أي تعديل جديد على دالة التفعيل قد يجعل السكربت يفشل ويوقف البناء.
- يصعب اختبار النسخة الحقيقية محليًا أو مراجعتها بثقة.

**الأولوية الأولى:** نقل السلوك النهائي إلى `lib/main.dart` نفسه، ثم إيقاف التعديل النصي أثناء البناء بعد التحقق من تطابق السلوك.

### 2. مشروع Android يُنشأ من جديد في كل Build

الـworkflow ينفذ:

```text
flutter create --platforms=android --org com.explapp --no-pub .
```

ثم ينسخ ملفات Native إلى المشروع الناتج. هذه الطريقة تعمل للبناء السريع، لكنها تزيد مخاطر:

- اختلاف إعدادات Gradle بين إصدارات Flutter.
- تغيّر ملفات Android تلقائيًا من دون مراجعة.
- صعوبة اختبار المشروع الكامل قبل الرفع.
- عدم وجود مشروع Android ثابت يمكن تحليله ومقارنته.

سيبقى الأسلوب الحالي مؤقتًا إلى أن يثبت التطبيق، لكن يجب أن تكون جميع الملفات التي تغيّر السلوك موجودة بوضوح داخل المستودع، وألا يعتمد البناء على ترقيع نصي خفي.

### 3. عداد الاستخدام يعتمد على مرور ثانية والشاشة تفاعلية

الخدمة تزيد `used_seconds` كل ثانية عندما تكون الشاشة تفاعلية والحماية مفعلة. هذا يعني أن القياس الحالي هو تقريب لوقت تشغيل شاشة الهاتف، وليس قياسًا مؤكدًا لاستخدام التطبيقات.

المخاطر:

- تأخر `Handler` أثناء ضغط النظام أو وضع توفير الطاقة.
- اختلاف الوقت الفعلي عن عدد مرات تنفيذ المؤقت.
- فقد ثوانٍ أو دقائق عند إيقاف الخدمة مؤقتًا.
- الاعتماد على ساعة التاريخ المحلية لإعادة التعيين اليومي.
- إمكانية التأثر بتغيير تاريخ أو وقت الهاتف.

**خطة الإصلاح:** استخدام نقطة زمنية رتيبة مثل `SystemClock.elapsedRealtime()` لحساب الفارق الحقيقي بين كل دورتين، مع حفظ آخر نقطة مزامنة، بدل إضافة ثانية ثابتة لكل تشغيل للمؤقت.

### 4. التفعيل لا يضمن جاهزية شاشة القفل

يمكن تشغيل العداد حتى عندما تكون صلاحية الظهور فوق التطبيقات غير مفعلة. هذا قد يجعل الوقت ينتهي من دون ظهور شاشة القفل.

المطلوب ليس منع تشغيل العداد، بل إظهار حالة واضحة للمستخدم:

- الحماية والعداد يعملان.
- شاشة القفل غير جاهزة.
- ما الصلاحية الناقصة؟
- زر مباشر لفتح الإعداد الصحيح.
- اختبار فوري لشاشة القفل قبل الاعتماد على التطبيق.

### 5. لا توجد صفحة تشخيص كاملة

التطبيق يحتاج إلى شاشة تعرض على الأقل:

- هل الخدمة تعمل الآن؟
- وقت آخر نبضة من الخدمة.
- هل Overlay مسموح؟
- هل الإشعارات مسموحة؟
- هل التطبيق Device Owner؟
- هل Device Admin فعال؟
- هل Lock Task مسموح؟
- آخر مرة بدأت فيها الخدمة.
- آخر مرة توقفت فيها الخدمة.
- آخر محاولة لإظهار شاشة القفل.
- آخر خطأ عند فتح شاشة القفل.
- سبب إعادة تشغيل الخدمة.

### 6. إشعارات Android 13 وما بعده

الـManifest يحتوي على `POST_NOTIFICATIONS`، لكن يجب التحقق من طلب الصلاحية أثناء التشغيل. من دون ذلك قد لا تظهر تحذيرات الخمس دقائق والدقيقة أو إشعار الحماية بالشكل المتوقع.

### 7. PIN محفوظ كنص مباشر

رمز ولي الأمر محفوظ حاليًا داخل `SharedPreferences` كنص مباشر. يلزم لاحقًا:

- تخزين hash مع salt بدل الرمز نفسه.
- ترحيل الرمز القديم دون تعطيل المستخدم.
- تأخير متزايد بعد المحاولات الفاشلة.
- عدم تضمين أي بريد افتراضي شخصي داخل الكود.

### 8. لا يوجد اختبار واقعي كامل لمسار الحماية

نجاح `flutter analyze` وبناء APK لا يثبت أن الرقابة تعمل. يجب اختبار السيناريو كاملًا على هاتف حقيقي.

## مصفوفة الاختبار المطلوبة

### التفعيل الأساسي

1. تثبيت التطبيق لأول مرة.
2. إنشاء PIN.
3. اختيار 10 دقائق.
4. تشغيل الحماية.
5. منح Overlay.
6. التحقق من استمرار العداد بعد الخروج من التطبيق.
7. إغلاق التطبيق من قائمة التطبيقات الحديثة.
8. إطفاء الشاشة وتشغيلها.
9. انتظار انتهاء الوقت.
10. التأكد من ظهور شاشة القفل.

### إعادة التشغيل

1. تشغيل الحماية.
2. إعادة تشغيل الهاتف.
3. عدم فتح التطبيق يدويًا.
4. التأكد من بدء الخدمة.
5. التأكد من استمرار الوقت السابق وعدم تصفيره دون سبب.
6. اختبار القفل بعد انتهاء الحد.

### الصلاحيات

- سحب صلاحية Overlay أثناء تشغيل الحماية.
- منع الإشعارات.
- إعادة منح الصلاحيات.
- اختبار Device Admin.
- اختبار Device Owner على جهاز تجريبي منفصل.

### مقاومة التحايل

- تغيير الساعة إلى الأمام.
- تغيير الساعة إلى الخلف.
- تغيير التاريخ.
- Force Stop.
- إعادة تشغيل الهاتف.
- فتح شاشة التطبيقات الحديثة.
- الضغط على Home وBack.
- محاولة حذف التطبيق.

### دقة الوقت

اختبارات لمدة:

- 10 دقائق.
- 30 دقيقة.
- ساعة.

ويتم في كل اختبار مقارنة:

- الزمن الحقيقي بساعة مستقلة.
- `used_seconds` داخل التطبيق.
- زمن تشغيل الشاشة من النظام عند توفره.
- مقدار الانحراف.

الحد المستهدف بعد الإصلاح: انحراف أقل من 1% في التشغيل الطبيعي، مع تسجيل أي فترة توقفت فيها الخدمة بدل إخفائها.

## ترتيب الإصلاحات

1. توحيد الكود المصدر مع الكود الذي يبنيه workflow.
2. إضافة سجل تشخيص لحياة الخدمة والقفل والصلاحيات.
3. إصلاح حساب الوقت باستخدام الفارق الزمني الحقيقي.
4. إضافة فحص وطلب صلاحية الإشعارات.
5. إضافة اختبار شاشة القفل من داخل التطبيق.
6. إصلاح إعادة التشغيل والاستعادة بعد Boot.
7. تحسين PIN وإزالة البريد الافتراضي الشخصي.
8. إجراء اختبارات هاتف حقيقي وتوثيق النتائج حسب الشركة وإصدار Android.

## قاعدة العمل

لن يتم اعتماد تعديل كبير دفعة واحدة. كل تغيير يجب أن:

- يعالج مشكلة محددة.
- يحافظ على البيانات الحالية.
- يضيف تشخيصًا عند احتمال الفشل.
- يمر عبر التحليل والبناء.
- يكون قابلًا للتراجع والمراجعة.
