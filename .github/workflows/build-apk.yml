name: Build and Verify KidsMonnter APK

on:
  workflow_dispatch:
  pull_request:
    branches: [main]
  push:
    branches: [main]
    paths-ignore:
      - 'apk/**'
      - 'DOWNLOAD_APK.md'

concurrency:
  group: kidsmonnter-apk-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 35

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.sha || github.ref }}

      - name: Validate Python merge tools
        run: python3 -m py_compile tools/*.py

      - name: Merge protection hardening into build sources
        run: |
          set +e
          {
            python3 tools/merge_preflight_hardening.py
            python3 tools/merge_service_watchdog.py
            python3 tools/merge_service_restart_channel.py
            python3 tools/merge_status_self_heal.py
            python3 tools/merge_service_restart_cooldown.py
            python3 tools/merge_notification_permission.py
            python3 tools/merge_guard_notification_navigation.py
            python3 tools/merge_lock_launch_cooldown.py
            python3 tools/merge_background_lock_overlay_v2.py
            python3 tools/merge_safe_lock_keypad.py
            python3 tools/merge_lock_emergency_stop.py
            python3 tools/merge_safe_policy_release.py
            python3 tools/merge_runtime_diagnostics.py
            python3 tools/merge_runtime_diagnostics_contract_compat.py
            python3 tools/merge_strict_runtime_resilience.py
          } > merge-diagnostics.log 2>&1
          status=$?
          cat merge-diagnostics.log
          exit $status

      - name: Upload merge diagnostics
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: merge-diagnostics-${{ github.sha }}
          path: merge-diagnostics.log
          if-no-files-found: error
          retention-days: 2

      - name: Verify critical protection contracts
        run: |
          test -f native/MainActivityV2.kt
          test -f native/AndroidManifest.xml
          test -f test/guard_status_test.dart
          test -f test/service_watchdog_contract_test.dart
          test -f test/status_self_heal_contract_test.dart
          test -f test/service_restart_cooldown_contract_test.dart
          test -f test/notification_permission_contract_test.dart
          test -f test/guard_notification_navigation_contract_test.dart
          test -f test/lock_launch_cooldown_contract_test.dart
          test -f test/background_lock_overlay_contract_test.dart
          test -f test/safe_lock_keypad_contract_test.dart
          test -f test/lock_emergency_stop_contract_test.dart
          test -f test/safe_policy_release_contract_test.dart
          test -f test/runtime_diagnostics_contract_test.dart
          test -f test/strict_runtime_resilience_contract_test.dart
          grep -q 'uninstallBlocked' lib/main.dart
          grep -q 'PendingIntent.getBroadcast' native/MainActivityV2.kt
          grep -q 'MONITOR_WATCHDOG_ACTION' native/MainActivityV2.kt
          grep -q 'requestNotificationPermissionIfNeeded' native/MainActivityV2.kt
          grep -q 'guardNotificationContentIntent' native/MainActivityV2.kt
          grep -q 'LOCK_LAUNCH_COOLDOWN_MS' native/MainActivityV2.kt
          grep -q 'BACKGROUND_LOCK_OVERLAY_MARKER' native/MainActivityV2.kt
          grep -q 'TYPE_APPLICATION_OVERLAY' native/MainActivityV2.kt
          grep -q 'manager.addView(view, params)' native/MainActivityV2.kt
          grep -q 'RUNTIME_DIAGNOSTICS_MARKER' native/MainActivityV2.kt
          grep -q 'SERVICE_HEARTBEAT' native/MainActivityV2.kt
          grep -q 'TICK_ERROR' native/MainActivityV2.kt
          grep -q 'RUNTIME_DIAGNOSTICS_UI_MARKER' lib/main.dart
          grep -q 'getDiagnosticLog' lib/main.dart
          grep -q 'STRICT_RUNTIME_RESILIENCE_MARKER' native/MainActivityV2.kt
          grep -q 'setExactAndAllowWhileIdle' native/MainActivityV2.kt
          grep -q 'createDeviceProtectedStorageContext' native/MainActivityV2.kt
          grep -q 'scheduleBootRetries' native/MainActivityV2.kt
          grep -q 'STRICT_RUNTIME_UI_MARKER' lib/main.dart
          grep -q 'android.permission.SCHEDULE_EXACT_ALARM' native/AndroidManifest.xml
          grep -q 'android:directBootAware="true"' native/AndroidManifest.xml
          grep -q 'GridLayout(this)' native/MainActivityV2.kt
          grep -q 'unlockWithPin(addTime = true)' native/MainActivityV2.kt
          grep -q 'unlockWithPin(addTime = false)' native/MainActivityV2.kt
          grep -q 'disableProtectionWithPin()' native/MainActivityV2.kt
          grep -q 'releaseDeviceOwnerPolicies()' native/MainActivityV2.kt
          grep -q 'text = "إيقاف الحماية"' native/MainActivityV2.kt
          ! sed -n '/class MonitorService/,/class LockActivity/p' native/MainActivityV2.kt | grep -q 'startActivity(Intent(this, LockActivity::class.java)'
          ! sed -n '/class LockActivity/,/class KidsMonnterDeviceAdminReceiver/p' native/MainActivityV2.kt | grep -q 'EditText(this)'
          ! sed -n '/class LockActivity/,/class KidsMonnterDeviceAdminReceiver/p' native/MainActivityV2.kt | grep -q 'FLAG_TURN_SCREEN_ON'
          ! grep -q 'PendingIntent.getService' native/MainActivityV2.kt

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Generate Android project files
        run: flutter create --platforms=android --org com.explapp --no-pub .

      - name: Install native monitoring implementation
        run: |
          rm -f test/widget_test.dart
          mkdir -p android/app/src/main/kotlin/com/explapp/kidstimeguard
          cp native/MainActivityV2.kt android/app/src/main/kotlin/com/explapp/kidstimeguard/MainActivity.kt
          cp native/AndroidManifest.xml android/app/src/main/AndroidManifest.xml
          mkdir -p android/app/src/main/res/xml android/app/src/main/res/values
          cp native/res/xml/device_admin_receiver.xml android/app/src/main/res/xml/device_admin_receiver.xml
          cp native/res/values/device_admin_strings.xml android/app/src/main/res/values/device_admin_strings.xml
          rm -rf android/app/src/main/kotlin/com/explapp/kidsmonnter
          sed -i 's/com.explapp.kidsmonnter/com.explapp.kidstimeguard/g' android/app/build.gradle.kts
          sed -i 's/minSdk = flutter.minSdkVersion/minSdk = 23/g' android/app/build.gradle.kts

      - name: Install Flutter packages
        run: flutter pub get

      - name: Format Dart sources in build workspace
        run: dart format lib test

      - name: Analyze application
        run: flutter analyze --no-fatal-infos --no-fatal-warnings

      - name: Run tests
        run: |
          set +e
          flutter test > flutter-test.log 2>&1
          status=$?
          cat flutter-test.log
          exit $status

      - name: Upload Flutter test diagnostics
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: flutter-test-diagnostics-${{ github.sha }}
          path: flutter-test.log
          if-no-files-found: error
          retention-days: 2

      - name: Build release APK
        run: flutter build apk --release

      - name: Prepare APK artifact
        run: |
          mkdir -p dist
          cp build/app/outputs/flutter-apk/app-release.apk dist/KidsMonnter.apk

      - name: Upload verified APK
        uses: actions/upload-artifact@v4
        with:
          name: KidsMonnter-${{ github.sha }}
          path: dist/KidsMonnter.apk
          if-no-files-found: error
          retention-days: 14

      - name: Publish verified source and APK on main
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          mkdir -p apk
          cp dist/KidsMonnter.apk apk/KidsMonnter.apk
          printf '# تحميل التطبيق\n\n[تحميل KidsMonnter APK](./apk/KidsMonnter.apk)\n' > DOWNLOAD_APK.md
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add lib/main.dart native/MainActivityV2.kt native/AndroidManifest.xml apk/KidsMonnter.apk DOWNLOAD_APK.md
          git diff --cached --quiet || git commit -m "بناء نسخة KidsMonnter الآمنة وتحديث APK"
          git push origin HEAD:main
